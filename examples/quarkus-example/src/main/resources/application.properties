# Bind to all interfaces inside the container (not just 127.0.0.1).
quarkus.http.host=0.0.0.0
quarkus.http.port=8080

# Make startup logs more useful when watching with `podman logs -f`.
quarkus.log.console.format=%d{HH:mm:ss.SSS} %-5p [%c{2.}] %s%e%n
